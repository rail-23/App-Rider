import React from 'react';

const Settings = () => (
  <div className="settings-page">
    <h2 className="text-2xl font-semibold mb-4">Configuración</h2>
    <p className="muted">Ajustes de la aplicación y preferencias.</p>
  </div>
);


export default Settings;
